namespace Adanom.Ecommerce.API
{
    public enum ValidationErrorCodesEnum
    {
        REQUIRED = 0,
        NULL = 1,
        NOT_EXISTS = 2,
        GREATER_THAN = 3,
        LESS_THAN = 4,
        NOT_ALLOWED = 5,
    }
}
